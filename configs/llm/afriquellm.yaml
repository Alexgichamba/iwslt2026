# AfriqueLLM — primary target LLM for IWSLT submission
model_name_or_path: "McGill-NLP/AfriqueGemma-4B"
dtype: bfloat16
freeze: true

# LoRA config — set to null to disable, or specify params
lora:
  r: 16
  lora_alpha: 32
  lora_dropout: 0.05
  target_modules:
    - q_proj
    - v_proj
    - k_proj
    - o_proj
